# 개발 체크리스트 (완료 vs 남은 작업)

작성일: 2026.02.13  
참조: PRD.md, progress.md, plan-admin.md, plan-events-operations.md

---

## ✅ 완료된 것

### Phase 1 (MVP)
- [x] 사용자 DB·OAuth 연동
- [x] 메인 대시보드(레벨, 포인트)·기부(목표/마감)
- [x] 관리자 기본 페이지 (사용자 목록, 포인트 지급, 메인 문구, 관리자 토글)

### Phase 2 – 이벤트·관리자
- [x] 이벤트 목록 `/admin/events`
- [x] 이벤트 등록 `/admin/events/new`
- [x] 이벤트 상세 `/admin/events/[eventId]` — 소개문구·상태·보상 금액 수정 (EditEventForm), 구간 추가/삭제
- [x] 관리자 대시보드 지표 (전사 기부금, 목표 달성률, 승인 대기 건수, 기간별 기부)
- [x] 인증 심사 센터 `/admin/verifications` (대량 승인/반려, 이벤트·구간 필터)
- [x] 기부처 관리 `/admin/donation-targets`
- [x] **반려 알림**: 이벤트 카드/상세에 "반려됨" 표시, 마이페이지 "이벤트 참여 내역"에서 반려 사유 확인

### 이벤트 참여·보상
- [x] 사용자 챌린지 참여 (인증 모달·동적 폼: 사진/텍스트/숫자/동료 선택)
- [x] SEASONAL 구간별 상태 (미오픈/인증가능/승인 대기중/보상대기/완료/마감/반려됨)
- [x] **칭찬 챌린지 쌍방 지급**: 승인 시 참여자+수신자(peer) 둘 다 포인트 지급 (reward_policy BOTH)
- [x] 승인 시 포인트 지급 완료 메시지, 마이페이지 포인트 내역(기부/적립 필터)
- [x] **보상 선택 (CHOICE/복수 보상)**: 승인 후 사용자가 V.Credit/커피 쿠폰/굿즈 중 선택 → 포인트 즉시 지급 또는 쿠폰/굿즈는 관리자 발송 대상으로 기록
- [x] **관리자 쿠폰/굿즈 발송 대상** `/admin/reward-fulfillment`: 비포인트 보상 선택 건 한눈에 조회
- [x] **ALWAYS 빈도 제한**: 일/주/월 1회 참여 제한. 제출 전 서버 검사 + 모달에서 "오늘 이미 참여했습니다" 등 안내 (구간제와 분리: SEASONAL은 구간별만, ALWAYS만 빈도 적용)

### Phase 3 (2026.02.23)
- [x] **V.Honors**: 메인 페이지에만 TOP 10 개인/팀 랭킹 (전체 보기·전용 페이지 제거)
- [x] **모바일 UX**: 랭킹 테이블 가로 스크롤, 터치 영역 44px (탭·하단 네비)
- [x] **MAU 지표**: 관리자 대시보드 "MAU (최근 30일)" 카드. `users.last_active_at` 컬럼 + 로그인/접속 시 갱신. 마이그레이션 `016-users-last-active-at.sql` 실행 후 숫자 표시, 미실행 시 "준비 중"

---

## ⏳ 남은 개발 (선택)

아래는 **기획서에는 있지만 아직 구현 안 된** 항목입니다. 필요할 때만 진행하면 됩니다.

| 항목 | 설명 | 우선순위 |
|------|------|----------|
| **이벤트 수정 전용 URL** | 현재는 상세 페이지(`/admin/events/[id]`)에서 수정. 별도 `/admin/events/[id]/edit` 페이지는 없음. (기능적으로는 충분) | 낮음 |
| **알림** | "칭찬이 도착했습니다" 등 알림 발송. 설계만 되어 있음. | 낮음 |
| **V.Honors Redis 캐싱** | 랭킹 집계 Redis 도입 시 선택. | 낮음 |
| **Vertex AI 이미지 분류** | 챌린지 인증샷 자동 식별 등. Phase 3. 별도 설계 후 진행. | Phase 3 |

---

## 📌 정리

- **지금 서비스 오픈에 꼭 필요한 기능은 모두 구현된 상태**입니다.  
  (기부, 이벤트 등록·참여·심사, 반려 알림, 기부처 관리, 관리자 대시보드)
- **보상 선택(CHOICE)**·**ALWAYS 빈도 제한**·**쿠폰/굿즈 발송 대상** 페이지는 구현 완료되었습니다.
- **Phase 3** 명예의 전당 TOP 10만 유지, MAU 지표·모바일 터치 UX 반영 완료. Vertex AI는 별도 설계 후 진행.
- **수동으로 해야 할 것**은 `당신이-할-일.md` 참고 (Supabase 마이그레이션 016 포함, 최초 관리자 지정, .env 설정).
